
#1.2.0 - added confreg function to construct confidence regions jointPredictiveSample.  Fixed bug in create.pgrid when midpoint == TRUE.

#1.1.2 - modified plot.pgrid so that is was insanely fast using the image function instead of manually plotting each pixel.